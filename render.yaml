services:
  - type: web
    name: i-kira-mail
    runtime: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate --noinput
    startCommand: gunicorn i-kira_mail.wsgi --log-file -
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DJANGO_SETTINGS_MODULE
        value: i-kira_mail.settings
      - key: DEBUG
        value: false
      - key: DATABASE_URL
        sync: false   # isi manual dari Neon dashboard
      - key: MAILTRAP_API_TOKEN
        sync: false
      - key: DEFAULT_FROM_EMAIL
        value: "i-kira Mail <noreply@i-kira.com>"
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
