from django.urls import path
from . import views

app_name = 'api'

urlpatterns = [
    # API endpoints akan ditambahkan nanti
    # path('emails/', views.EmailListAPI.as_view(), name='email_list'),
]